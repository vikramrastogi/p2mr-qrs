# Fuzz And Negative Tests Spec

Add malformed witness corpus and negative tests for witness shape, signature and
public-key lengths, CompactSize, control blocks, low bit, leaf version, Merkle
path, annex ambiguity, max depth, and fixed-length crypto failure.
