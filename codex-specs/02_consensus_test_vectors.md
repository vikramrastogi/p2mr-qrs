# Consensus Test Vector Spec

Create deterministic provisional fixtures for valid QRS spends, annex handling,
fixed-length crypto invalidity, structural invalidity, leaf-version mismatch,
Merkle mismatch, and maximum-depth paths. Validate failure-stage classification.
